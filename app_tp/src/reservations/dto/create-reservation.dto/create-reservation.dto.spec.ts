import { CreateReservationDto } from './create-reservation.dto';

describe('CreateReservationDto', () => {
  it('should be defined', () => {
    expect(new CreateReservationDto()).toBeDefined();
  });
});
